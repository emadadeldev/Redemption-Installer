<Window x:Class="Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Installer.Controls"
        mc:Ignorable="d"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FontFamily="pack://application:,,,/Assets/#Cairo"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        Title="MainWindow"
        Height="500" Width="900">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="20"        
            CornerRadius="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border BorderThickness="2" CornerRadius="25" BorderBrush="Red">

        <Border.Background>
            <ImageBrush ImageSource="Assets/wall.png" Stretch="Fill"/>
        </Border.Background>
        
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" MinWidth="468"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Button Content="×" 
            Click="CloseBtn"
            Grid.Row="0"
            Grid.Column="1"
            Cursor="Hand"
            Foreground="Red"
            FontSize="40"
            Margin="0 0 25 0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
        />
        
            <!--Content-->
            <ScrollViewer Height="auto" Width="auto" Grid.Column="1" Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <controls:names x:Name="names" Visibility="Visible" />
            </ScrollViewer>
            <!--Content-->

            <Grid Grid.Row="2" Grid.Column="1">
                <Button Content="التالي" Height="auto" Margin="20" HorizontalAlignment="Right" Width="130"/>
            </Grid>

        </Grid>
    </Border>

</Window>
