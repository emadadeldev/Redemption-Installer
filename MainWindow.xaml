<Window x:Class="EmadAdel.Redemption_Team.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:page="clr-namespace:Emadadel.Redemption_Team.path"
        WindowState="Normal"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="Redemption Team"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="Black">

    <Grid>

        <!-- Background Image -->
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Assets/wall.jpg"
                        Stretch="Fill"
                        Opacity="0.4"/>
        </Grid.Background>

        <!-- Close -->
        <TextBlock Text="🗙"
                    Grid.Row="0"
                    Margin="0,8,20,0"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    FontSize="25"
                    MouseLeftButtonDown="CloseBtn"
                    Foreground="#FFFF5B5B"/>

        <!-- Main -->
        <Grid Visibility="Visible" x:Name="MainContent" Margin="20">
            
        <Grid >

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!-- Welcome -->
            <StackPanel Grid.Row="0" Margin="0,20,0,0">

                <Image Source="Assets/logo.png"
                       Width="300"/>

                <TextBlock Text="المساهمين في التعريب"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="10"/>

                <ScrollViewer Height="200" Margin="0,0,1,0">
                    <StackPanel>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="اسم عشوائي" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                </ScrollViewer>

            </StackPanel>

            <!-- Bottom -->
                <Grid Grid.Row="2">
                    <Button  Click="Button_Options_Click"
                        Content="التالي"
                        Width="100"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Height="35"/>
                </Grid>
        </Grid>
    </Grid>

        <Border x:Name="options"
       Panel.ZIndex="-1"
       Visibility="Collapsed">
            <page:InstallerControl />
        </Border>
    </Grid>
</Window>
