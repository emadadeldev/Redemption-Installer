<Window x:Class="EmadAdel.Redemption_Team.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:EmadAdel.Redemption_Team.Controls"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowState="Normal"
        Title="Redemption Team"
        Height="500"
        Width="850"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <!-- Border fills entire window -->
    <Border Background="Black" CornerRadius="20" SnapsToDevicePixels="True">
        <Grid>
            <!-- Background Image with Blur -->
            <Image Source="pack://application:,,,/Assets/wall.jpg"
                   Stretch="Fill"
                   Opacity="0.6"/>

            <!-- Close Button -->
            <TextBlock Text="🗙"
                       Cursor="Hand"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Margin="0,10,15,0"
                       Panel.ZIndex="3"
                       FontSize="24"
                       Foreground="#FFFF5B5B"
                       MouseLeftButtonDown="CloseBtn"
                       ToolTip="إغلاق"/>

            <!-- Main Content -->
            <Grid x:Name="MainContent" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo and Title -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Image Source="Assets/logo.png" Width="180"/>
                    <TextBlock Text="فريق التعريب"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="5"/>
                    <TextBlock x:Name="Contributor_Text"
                               Text="...جاري تحميل قائمة المساهمين"
                               Foreground="LightGray"
                               HorizontalAlignment="Center"
                               FontSize="16"
                               Margin="5"/>
                </StackPanel>

                <!-- Contributors List -->
                <Border Grid.Row="1"
                        Background="#66000000"
                        CornerRadius="10"
                        Padding="5"
                        HorizontalAlignment="Center"
                        Width="450"
                        Height="250">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ContributorsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#22000000" CornerRadius="5" Margin="3" Padding="5">
                                        <TextBlock Text="{Binding}" 
                                                   FontWeight="SemiBold" 
                                                   FontSize="18" 
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Bottom Button -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button x:Name="Nextbtn"
                            Visibility="Collapsed"
                            Click="Button_Options_Click"
                            Content="التالي"
                            Width="100"
                            Height="35"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Options Panel -->
            <Border x:Name="options"
                    Panel.ZIndex="2"
                    Visibility="Collapsed">
                <controls:Path />
            </Border>

        </Grid>
    </Border>
</Window>
